import { GetFirstUpperCharPipe } from './get-first-upper-char.pipe';

describe('GetFirstUpperCharPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirstUpperCharPipe();
    expect(pipe).toBeTruthy();
  });
});
